const producto = (id,stock,nombre,precio) => {
    this.id = id;
    this.stock = stock;
    tihs.nombre = nombre;
    this.precio = precio;
}

const Tienda_Virtual = () => {
    const inventario = () => {
        const Camisa = () => {
        const  prototype = producto(1,2,"Camisa",10);
        return Object.assign({},prototype,);
        };
        const Pantalon = () => {
            const prototype = producto(2,2,"Pantalon",13);
        return Object.assign({},prototype);
        };
        const Zapatos = () => {
        const prototype = producto(3,2,"Zapatos",20);
        return Object.assign({},prototype);
        }
    }
    const carritoDeCompras = () => {

    }
    const agregarProducto = (producto) => {

    }
}